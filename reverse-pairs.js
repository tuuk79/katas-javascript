function getReversePairs(input) {
  input.map(function(element, index) {
console.log(index)
});
}

function getSub(input, i) {
  return input.slice(i);
}

module.exports = { getReversePairs, getSub };
